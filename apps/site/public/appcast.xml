<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Bouclier.ai Updates</title>
    <link>https://www.bouclier.ai/appcast.xml</link>
    <description>Updates for Bouclier.ai — prompt injection firewall for macOS</description>
    <language>en</language>
    <item>
      <title>Version 0.9.2</title>
      <pubDate>Sun, 09 Aug 2026 07:50:13 +0200</pubDate>
      <sparkle:version>0.9.2</sparkle:version>
      <sparkle:shortVersionString>0.9.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h2>Bouclier.ai v0.9.2</h2>
<h4>Changed</h4>
<ul>
<li><strong>Monitor by default; blocking is opt-in.</strong> The gateway used to refuse a flagged request outright, which meant a false positive on tool output could wedge a normal agent session (the whole conversation is resent each turn, so one benign-but-matching result kept failing on every retry). Bouclier now <strong>detects and logs by default and forwards the request</strong> — it can't break normal work. Turn on enforcement to get refusals: <code>defaults write ai.bouclier.app injectionBlockEnabled -bool true</code>, or push it fleet-wide by MDM. Your own prompts are never blocked in either mode.</li>
</ul>
<h4>Removed</h4>
<ul>
<li><strong>The secret keeper is gone.</strong> The feature that stored managed credentials in your Keychain, scrubbed their real values out of outbound requests, and restored them in the response has been removed entirely — along with its agent secret-request flow, the <code>bouclier secrets</code> / <code>bouclier env</code> / <code>bouclier protection</code> CLI commands, and the secrets MCP server. It was off by default, under-tested, and marginal in practice (API keys live in request headers, not prompt bodies). Bouclier is now a focused prompt-injection firewall. The gateway is a byte-faithful transparent relay: it forwards a request unmodified or refuses it, and never rewrites. The <code>bouclier</code> CLI is read-only (<code>status</code>, <code>install</code>); the injection MCP server is unchanged. If you relied on the secret keeper, keep your credentials wherever your tools already read them.</li>
</ul>
<h4>Security</h4>
<ul>
<li>The security policy and threat model now describe the live attack surface accurately: the injection firewall is the primary control (they previously described it as dormant), and the secret-scrub invariants are gone. Public contact is <code>apps@superstellar.io</code>, and the <code>/blocked</code> page and <code>SECURITY.md</code> were trimmed to avoid over-exposing detection internals.</li>
</ul>
]]></description>
      <enclosure
        url="https://0tdi95zyjwsefpzx.public.blob.vercel-storage.com/download/Bouclier-ai-v0.9.2-macOS.dmg"
        sparkle:edSignature="lwTYnHo0GGaZj6HQaCQlR+k7iYMbkzMOWvGHwn8httc0IUqtEIp0V4P5yW3Hk2jMXEhdZ8+zWV6pSd55JDadDA=="
        length="272174324"
        type="application/octet-stream"
      />
    </item>
  </channel>
</rss>
