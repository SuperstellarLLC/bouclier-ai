<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Bouclier.ai Updates</title>
    <link>https://www.bouclier.ai/appcast.xml</link>
    <description>Updates for Bouclier.ai — prompt injection firewall for macOS</description>
    <language>en</language>
    <item>
      <title>Version 0.7.0</title>
      <pubDate>Thu, 02 Jul 2026 18:10:38 +0200</pubDate>
      <sparkle:version>0.7.0</sparkle:version>
      <sparkle:shortVersionString>0.7.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<h2>Bouclier.ai v0.7.0</h2>
<h4>Removed</h4>
<ul>
<li><strong>Extreme mode is gone.</strong> The CA-based "full interception" mode — installing a trusted root certificate and a System Extension to decrypt and inspect all AI traffic system-wide — has been removed entirely. Standard mode (the certificate-free gateway your agent's SDK points at via <code>ANTHROPIC_BASE_URL</code>/<code>OPENAI_BASE_URL</code>) is now the only way Bouclier runs. If you had extreme mode enabled, this update automatically uninstalls the CA from your Keychain, deactivates the System Extension, and clears any leftover system proxy configuration — no action needed.</li>
<li><strong>Prompt-injection and attachment PII detection are no longer active.</strong> That engine only ever ran through extreme mode's interception path, so removing extreme mode removes it from your outbound traffic too. The secret keeper (scrub real credential values before they reach the model, restore them in the response) is unaffected and remains Bouclier's primary protection. Settings → Privacy (the attachment-scanning toggle and redaction audit) is gone with it.</li>
<li><strong>The on-device ML models are no longer bundled.</strong> Nothing has called them since extreme mode's removal, so the app no longer ships ~600MB of model weights it never uses. A typical install is now ~6MB instead of ~600MB.</li>
</ul>
<h4>Changed</h4>
<ul>
<li>Settings → Protection is simplified to a single mode: no more mode picker, CA status, or System Extension status. It now shows gateway status, certificate status (not needed), and secret-keeper status.</li>
<li>Patched all known dependency vulnerabilities (<code>vitest</code>, <code>vite</code>, <code>undici</code>, <code>esbuild</code>, <code>@babel/core</code>, <code>js-yaml</code>).</li>
</ul>
]]></description>
      <enclosure
        url="https://0tdi95zyjwsefpzx.public.blob.vercel-storage.com/download/Bouclier-ai-v0.7.0-macOS.dmg"
        sparkle:edSignature="lpfjpPo49TBebynGwU/aSVq+m1BU0GJ0628SwRXmaA+P88X4DQbyQiZOu61Tr3rDYmzNnSjwyVFFVISNDA8EAQ=="
        length="9113239"
        type="application/octet-stream"
      />
    </item>
  </channel>
</rss>
